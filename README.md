# MP Android Security

MVP de scanner local Android em Kotlin + Jetpack Compose.

O workflow .github/workflows/android-debug.yml gera app-debug.apk no GitHub Actions e publica o APK como artifact.

Requer JDK 17, Android SDK 35 e Gradle 8.10.2 via Wrapper.
